require "spec_helper"

RSpec.describe Fast do
  describe "execute" do
    it "returns an OpenStruct" do
      # alignment = File.open("spec/fixtures/fast/valid_input/ali.phy").read
      # tree = File.open("spec/fixtures/fast/valid_input/ali.nwk").read
      # out = Fast.new.execute({input: {alignment: alignment, tree: tree}})
      # expect(out.successful).to be true
      # expect(out.output.class).to eq(String)
    end
  end
end
